---
aliases: ["Приём · дополнительная_точка", "дополнительная_точка"]
cssclasses: [ege-moc, ege-technique]
tags: [ege/moc/technique, ege/technique/дополнительная_точка]
technique: дополнительная_точка
---

# Приём · дополнительная_точка

> [!abstract] Описание
> Ввести дополнительную точку с условием на отношение

## 🗂 Задачи с этим приёмом

```dataview
TABLE WITHOUT ID file.link AS Задача, subject_ru AS Предмет, kes_code AS КЭС, difficulty AS Сл, status AS Статус
FROM #ege/technique/дополнительная_точка AND -#ege/moc
SORT difficulty DESC, date DESC
```

## 📊 Успешность по приёму

```dataview
TABLE WITHOUT ID subject_ru AS Предмет, length(rows) AS Всего, length(filter(rows, (r) => r.status = "pass")) AS ✓
FROM #ege/technique/дополнительная_точка AND -#ege/moc
GROUP BY subject_ru
```

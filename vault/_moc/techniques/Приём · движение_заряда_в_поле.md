---
aliases: ["Приём · движение_заряда_в_поле", "движение_заряда_в_поле"]
cssclasses: [ege-moc, ege-technique]
tags: [ege/moc/technique, ege/technique/движение_заряда_в_поле]
technique: движение_заряда_в_поле
---

# Приём · движение_заряда_в_поле

> [!abstract] Описание
> Движение заряженной частицы в неоднородном E

## 🗂 Задачи с этим приёмом

```dataview
TABLE WITHOUT ID file.link AS Задача, subject_ru AS Предмет, kes_code AS КЭС, difficulty AS Сл, status AS Статус
FROM #ege/technique/движение_заряда_в_поле AND -#ege/moc
SORT difficulty DESC, date DESC
```

## 📊 Успешность по приёму

```dataview
TABLE WITHOUT ID key AS Предмет, length(rows) AS Всего, length(filter(rows, (r) => r.status = "pass")) AS ✓, length(filter(rows, (r) => r.status = "fail")) AS ✗
FROM #ege/technique/движение_заряда_в_поле AND -#ege/moc
WHERE subject_ru
GROUP BY subject_ru
SORT key ASC
```

---
aliases: ["Приём · построить_код_хэмминга", "построить_код_хэмминга"]
cssclasses: [ege-moc, ege-technique]
tags: [ege/moc/technique, ege/technique/построить_код_хэмминга]
technique: построить_код_хэмминга
---

# Приём · построить_код_хэмминга

> [!abstract] Описание
> Построить код Хэмминга для k бит данных

## 🗂 Задачи с этим приёмом

```dataview
TABLE WITHOUT ID file.link AS Задача, subject_ru AS Предмет, kes_code AS КЭС, difficulty AS Сл, status AS Статус
FROM #ege/technique/построить_код_хэмминга AND -#ege/moc
SORT difficulty DESC, date DESC
```

## 📊 Успешность по приёму

```dataview
TABLE WITHOUT ID key AS Предмет, length(rows) AS Всего, length(filter(rows, (r) => r.status = "pass")) AS ✓, length(filter(rows, (r) => r.status = "fail")) AS ✗
FROM #ege/technique/построить_код_хэмминга AND -#ege/moc
WHERE subject_ru
GROUP BY subject_ru
SORT key ASC
```

---
aliases: ["Приём · параметр_количество", "параметр_количество"]
cssclasses: [ege-moc, ege-technique]
tags: [ege/moc/technique, ege/technique/параметр_количество]
technique: параметр_количество
---

# Приём · параметр_количество

> [!abstract] Описание
> При каких параметрах уравнение f(x)=a имеет N корней

## 🗂 Задачи с этим приёмом

```dataview
TABLE WITHOUT ID file.link AS Задача, subject_ru AS Предмет, kes_code AS КЭС, difficulty AS Сл, status AS Статус
FROM #ege/technique/параметр_количество AND -#ege/moc
SORT difficulty DESC, date DESC
```

## 📊 Успешность по приёму

```dataview
TABLE WITHOUT ID subject_ru AS Предмет, length(rows) AS Всего, length(filter(rows, (r) => r.status = "pass")) AS ✓
FROM #ege/technique/параметр_количество AND -#ege/moc
GROUP BY subject_ru
```
